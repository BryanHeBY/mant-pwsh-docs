# Authoring templates

This directory contains recommended starting points for ManT-compatible
documents. Templates are guidance rather than generated inputs. Each template
starts with the conservative `Verification boundary` disposition; replace that
whole section with `Runtime evidence` only after recording an actual bounded
observation, and never retain both headings.
